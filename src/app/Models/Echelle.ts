export class Echelle {
  id: number;
  libelle: string;
  niveau: string;
  constructor(id: number, libelle: string, niveau: string) {
    this.id = id;
    this.libelle = libelle;
    this.niveau = niveau;
  }
}
